---
phase: 05-streaming-failure-semantics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/api/handlers/openai/openai_handlers.go
  - sdk/api/handlers/openai/openai_responses_handlers.go
  - sdk/api/handlers/gemini/gemini_handlers.go
  - sdk/api/handlers/gemini/gemini-cli_handlers.go
  - sdk/api/handlers/openai/openai_handlers_test.go
  - sdk/api/handlers/gemini/gemini_handlers_test.go
autonomous: true

must_haves:
  truths:
    - "OpenAI/Gemini terminal error writer calls c.Status(status) before writing the terminal error payload; if headers are already committed, client-visible status may remain 200 and failure classification relies on error_info"
  artifacts:
    - path: "sdk/api/handlers/openai/openai_handlers.go"
      provides: "WriteTerminalError sets status before writing SSE error payload"
    - path: "sdk/api/handlers/gemini/gemini_handlers.go"
      provides: "WriteTerminalError sets status before writing SSE/alt error payload"
  key_links:
    - from: "sdk/api/handlers/*/*_handlers.go"
      to: "sdk/api/handlers/stream_forwarder.go"
      via: "StreamForwardOptions.WriteTerminalError"
      pattern: "WriteTerminalError"
---

<objective>
对齐 OpenAI/Gemini 的 terminal error 写出顺序：在写出终止错误 payload 之前显式调用 `c.Status(status)`。

Purpose: 在“headers 尚未提交”的场景里让客户端/落库的 status_code 更接近真实失败；在“已 flush”的场景里依然依赖 Phase 05-01 的 `error_info` 作为强保证，从而避免 failure 污染 success 聚合。
Output: OpenAI/Gemini terminal error writer 统一在写出 payload 前调用 `c.Status(status)`，并新增最小回归测试锁定“未写入任何 chunk 即 terminal error”时的 status 行为。
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-streaming-failure-semantics/05-CONTEXT.md
@.planning/phases/05-streaming-failure-semantics/05-RESEARCH.md

@sdk/api/handlers/claude/code_handlers.go
@sdk/api/handlers/openai/openai_handlers.go
@sdk/api/handlers/openai/openai_responses_handlers.go
@sdk/api/handlers/gemini/gemini_handlers.go
@sdk/api/handlers/gemini/gemini-cli_handlers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAI streaming terminal error 在写 payload 前调用 c.Status(status)</name>
  <files>
sdk/api/handlers/openai/openai_handlers.go
sdk/api/handlers/openai/openai_responses_handlers.go
  </files>
  <action>
在 OpenAI 的 streaming 转发选项里（`StreamForwardOptions.WriteTerminalError` closure）：

- 计算 status：
  - 默认 `http.StatusInternalServerError`
  - 若 `errMsg.StatusCode > 0` 则使用它
- 在任何 `c.Writer.Write` / `fmt.Fprintf` 写出 error payload 之前调用 `c.Status(status)`。
- 保持现有 error payload 格式不变（OpenAI chat completions: `data: {error...}`；Responses SSE: `event: error\ndata: {error...}`）。
- closure 内仍然不要 flush（ForwardStream 负责 flush）。
  </action>
  <verify>go test ./...</verify>
  <done>
- OpenAI streaming terminal error 路径在写出终止错误 payload 前调用了 `c.Status(status)`（不承诺已 flush 的情况下客户端一定看到非 2xx）
  </done>
</task>

<task type="auto">
  <name>Task 2: Gemini streaming terminal error 在写 payload 前调用 c.Status(status)</name>
  <files>
sdk/api/handlers/gemini/gemini_handlers.go
sdk/api/handlers/gemini/gemini-cli_handlers.go
  </files>
  <action>
在 Gemini 的 streaming 转发选项里（`StreamForwardOptions.WriteTerminalError` closure）：

- 计算 status 规则同 OpenAI（默认 500，优先使用 `errMsg.StatusCode`）。
- 在写出 error payload 之前调用 `c.Status(status)`。
- 同时覆盖两类输出：
  - SSE (`alt == ""`)：写 `event: error` + `data: {error...}`
  - alt 非 SSE (`alt != ""`)：写 raw JSON error body
- 保持现有 body 构造逻辑不变，只补齐 status 设置。
  </action>
  <verify>go test ./...</verify>
  <done>
- Gemini streaming terminal error 路径在写出终止错误 payload 前调用了 `c.Status(status)`（SSE 与 alt 两分支都覆盖；不承诺已 flush 的情况下客户端一定看到非 2xx）
  </done>
</task>

<task type="auto">
  <name>Task 3: 新增回归测试：未写入任何 chunk 即 terminal error 时 status 会变为非 2xx</name>
  <files>
sdk/api/handlers/openai/openai_handlers_test.go
sdk/api/handlers/gemini/gemini_handlers_test.go
  </files>
  <action>
新增两组最小单测，验证“在未写入任何 streaming chunk 的情况下发生 terminal error”时，handler 会在写出 error payload 前把响应 status 设为对应的非 2xx 值。

OpenAI test（package `openai`）：
- 使用 `gin.CreateTestContext(httptest.NewRecorder())` 创建 context。
- 构造 handler：`NewOpenAIAPIHandler(&handlers.BaseAPIHandler{Cfg: &config.SDKConfig{}})`。
- 准备 `data` channel（不发送任何 chunk），`errs` channel（立即发送 `StatusCode: 401` 或 `500` 的 `ErrorMessage`）。
- 调用 `h.handleStreamResultWithPrefetched(...)`（test 与源码同 package，可调用未导出方法），等待其返回。
- 断言 `w.Code == 401`（或你指定的 status）。

Gemini test（package `gemini`）：
- 类似方式构造 handler，并调用 `forwardGeminiStreamWithPrefetched(..., alt="", ...)`，让 `errs` 先于任何 `data` 触发。
- 断言 `w.Code` 变为非 2xx。

注意：这些测试只锁定“未写入任何 chunk 的 terminal error”路径（headers 未提交）；已 flush 的路径依赖 `error_info` 分类（Plan 05-01）。
  </action>
  <verify>go test ./...</verify>
  <done>
- OpenAI/Gemini 各至少 1 个单测锁定：terminal error 在无数据写出时会把 status 设为非 2xx
  </done>
</task>

</tasks>

<verification>
- `go test ./...` 通过
- 代码审阅点：对齐 Claude 行为（`sdk/api/handlers/claude/code_handlers.go` 已有 `c.Status(status)`）
- 回归点：Plan 05-01 提供强保证（error_info），Plan 05-02 提供 best-effort 的 status 语义（headers 未提交时生效）
</verification>

<success_criteria>
- OpenAI/Gemini 的 terminal error writer 在写 payload 前都显式调用了 `c.Status(status)`
- 新增单测证明：在“未写入任何 chunk”时 status 会变为非 2xx
</success_criteria>

<output>
After completion, create `.planning/phases/05-streaming-failure-semantics/05-02-SUMMARY.md`
</output>

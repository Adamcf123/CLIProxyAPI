---
phase: 05-streaming-failure-semantics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/metrics_management_test.go
autonomous: true

must_haves:
  truths:
    - "Query API buckets mode classifies status_code=200 + error_info!=empty as failure (does not pollute success buckets)"
  artifacts:
    - path: "test/metrics_management_test.go"
      provides: "Regression coverage for bucket classification of 200+error_info failures"
  key_links:
    - from: "internal/api/handlers/management/metrics.go"
      to: "status_code + error_info"
      via: "success_flag CASE expression"
      pattern: "CASE WHEN status_code"
---

<objective>
为“流式失败不污染聚合”补上可回归的测试锁：在 buckets mode 下验证 `status_code=200` 但 `error_info` 非空的记录必须归类为 failure。

Purpose: buckets 聚合与 percentiles 同样依赖 success/failure 切分；需要显式覆盖该边界，防止回归。
Output: 扩展 buckets 测试数据集与断言，覆盖 200 + error_info 失败场景。
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/api/handlers/management/metrics.go
@test/metrics_management_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 buckets seed 数据：加入 200 + error_info 的 failure 行</name>
  <files>test/metrics_management_test.go</files>
  <action>
在 `seedBucketsMetricsDB` 的 rows 中新增一条 failure 行，用于模拟“HTTP status 可能仍是 200，但我们有可持久化 error_info”的流式失败：

- `status = 200`
- `errInfo = "boom"`（非空字符串）
- `createdAt` 落在与现有 `s_mid` / `f_mid` 相同的 bucket（当前是 00:10-00:15）

注意：该新增行应当只影响 failure bucket 的 count，不应改变 success bucket 的内容。
  </action>
  <verify>go test ./...</verify>
  <done>
- buckets 数据集中存在 1 条 `status_code=200` 且 `error_info` 非空的 failure row
  </done>
</task>

<task type="auto">
  <name>Task 2: buckets mode 断言：200 + error_info 不进入 success bucket</name>
  <files>test/metrics_management_test.go</files>
  <action>
在 `TestManagementMetrics_BucketsMode_AlignmentAndEmptyBuckets` 中补充断言：

- failure group 的“中间 bucket”（当前用 index 2 指向 00:10-00:15）`Count` 变为 2（包含 `f_mid` 与新增的 200+error_info 行）。
- success group 的同一 bucket `Count` 仍为 1（只包含 `s_mid`）。
- 可选但推荐：对 failure group 的该 bucket 增加 `Start == "2026-01-01T00:10:00Z"` 的断言，确保对齐一致。
  </action>
  <verify>go test ./...</verify>
  <done>
- buckets mode 明确锁定：`status_code=200` + `error_info != ''` => failure（不会污染 success buckets）
  </done>
</task>

</tasks>

<verification>
- `go test ./...` 通过
- 手工 spot-check：`internal/api/handlers/management/metrics.go` 的 `success_flag` 表达式同时用于 percentiles 和 buckets
- 说明：percentiles 路径已被既有测试数据隐式覆盖（`seedPercentilesMetricsDB` 包含 `status=200` + `errInfo="boom"` 的 failure 行）
</verification>

<success_criteria>
- buckets mode 覆盖并锁定 200 + error_info 的 failure 分类
</success_criteria>

<output>
After completion, create `.planning/phases/05-streaming-failure-semantics/05-03-SUMMARY.md`
</output>

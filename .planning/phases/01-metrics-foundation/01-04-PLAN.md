---
phase: 01-metrics-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - internal/metrics/collector.go
autonomous: true

must_haves:
  truths:
    - TPSCollector 可以按 provider/model 分组管理窗口
    - CompleteRequest 正确计算指标并添加到对应窗口
    - 可以查询任意分组的窗口统计
  artifacts:
    - path: internal/metrics/collector.go
      provides: TPSCollector 完整实现
      exports: [TPSCollector, CompleteRequest, GetWindowStats, GetAllKeys]
  key_links:
    - from: internal/metrics/collector.go
      to: internal/metrics/window.go
      via: TPSCollector.windows 使用 SlidingWindow
    - from: internal/metrics/collector.go
      to: internal/metrics/calculator.go
      via: CompleteRequest 调用计算函数
---

<objective>
完善 TPSCollector 集成滑动窗口和计算功能

Purpose: 将指标计算和滑动窗口聚合整合到 TPSCollector 中
Output: 完整的 collector.go 实现
</objective>

<execution_context>
@/home/adam/.claude/get-shit-done/workflows/execute-plan.md
@/home/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-metrics-foundation/01-CONTEXT.md
@.planning/phases/01-metrics-foundation/01-01-PLAN.md
@.planning/phases/01-metrics-foundation/01-02-PLAN.md
@.planning/phases/01-metrics-foundation/01-03-PLAN.md

## 集成要求

### 来自 Plan 01-01
- MetricKey 定义了分组键（provider + model + streaming）
- TPSCollector 结构体骨架

### 来自 Plan 01-02
- CalculateTTFT、CalculateTPS、CalculateTPOT 计算函数
- ValidateMetrics 验证函数

### 来自 Plan 01-03
- SlidingWindow 实现
- GetStats() 统计计算

### 边界情况（来自 CONTEXT.md）
- 请求超时/空响应：丢弃该请求，不记录任何指标
- 非流式请求：TTFT = 总响应时间（EndTime - StartTime）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完善 TPSCollector 结构体和方法</name>
  <files>internal/metrics/collector.go</files>
  <action>
更新 internal/metrics/collector.go：

1. 完善 TPSCollector 结构体：
   - windows map[MetricKey]*SlidingWindow
   - mu sync.RWMutex

2. 完善方法：
   - CompleteRequest(m *RequestMetrics, outputTokens int) error
     - 调用 calculator.go 的计算函数计算 TPS、TTFT、TPOT
     - 如果计算失败，返回错误（不记录该请求）
     - 获取或创建对应 MetricKey 的 SlidingWindow
     - 将 metrics 添加到窗口
     - 返回 nil

   - GetWindowStats(key MetricKey) (WindowStats, bool)
     - 查找对应 key 的窗口
     - 如果不存在，返回 false
     - 调用 window.GetStats() 返回统计值

   - GetOrCreateWindow(key MetricKey) *SlidingWindow
     - 内部方法，获取或创建窗口

3. 添加辅助方法：
   - GetAllKeys() []MetricKey - 返回所有有数据的 key

注意：CompleteRequest 需要处理非流式请求的 TTFT 计算（等于总响应时间）。
  </action>
  <verify>go build ./internal/metrics/... 成功编译</verify>
  <done>collector.go 更新完成，集成滑动窗口和计算功能</done>
</task>

<task type="auto">
  <name>Task 2: 添加 TPSCollector 单元测试</name>
  <files>internal/metrics/collector_test.go</files>
  <action>
创建 internal/metrics/collector_test.go，编写以下测试：

1. TestTPSCollector_StartRequest
   - 创建 collector，开始一个请求
   - 验证返回的 RequestMetrics 包含正确的 TrackingID 和 Key

2. TestTPSCollector_CompleteRequest
   - 开始请求，记录首 token 时间，完成请求
   - 验证指标计算正确（TPS、TTFT、TPOT）
   - 验证指标被添加到对应窗口

3. TestTPSCollector_GetWindowStats
   - 完成多个请求
   - 验证可以获取窗口统计
   - 验证不存在的 key 返回 false

4. TestTPSCollector_InvalidRequest
   - 0 token 的请求返回错误
   - 验证该请求未被记录到窗口

5. TestTPSCollector_GetAllKeys
   - 完成多个不同 key 的请求
   - 验证 GetAllKeys 返回所有 key

使用 testify/assert 进行断言。
  </action>
  <verify>go test ./internal/metrics/... -v 所有测试通过</verify>
  <done>collector_test.go 创建完成，测试覆盖率 >80%</done>
</task>

</tasks>

<verification>
- TPSCollector 可以管理多个滑动窗口（按 key 分组）
- CompleteRequest 正确计算并存储指标
- GetWindowStats 可以查询任意分组的统计
- 边界情况正确处理（0 token、无效时间）
- 所有测试通过
</verification>

<success_criteria>
- collector.go 包含完整的 TPSCollector 实现
- collector_test.go 包含完整的单元测试
- 所有测试通过
- 代码可以通过 go build 编译
</success_criteria>

<output>
After completion, create `.planning/phases/01-metrics-foundation/01-04-SUMMARY.md`
</output>

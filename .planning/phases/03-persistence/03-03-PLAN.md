---
phase: 03-persistence
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - internal/metricspersist/db.go
  - internal/metricsruntime/usage_plugin.go
  - internal/metricslog/jsonl_writer.go
  - internal/metricslog/types.go
  - cmd/server/main.go
autonomous: true
must_haves:
  truths:
    - "Only 7 days of metrics data are kept in the database"
    - "JSONL metrics logs are no longer written to disk"
    - "Legacy metricslog package is decommissioned"
  artifacts:
    - path: "internal/metricspersist/db.go"
      provides: "Retention cleanup logic"
  key_links:
    - from: "internal/metricsruntime/usage_plugin.go"
      to: "internal/metricslog"
      via: "Removed dependency"
---

<objective>
Implement data retention policy and decommission the legacy JSONL logging system.

Purpose: Maintain database size and establish SQLite as the single source of truth for metrics.
Output: A self-cleaning database and a cleaner codebase without legacy JSONL logic.
</objective>

<execution_context>
@/home/adam/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-persistence/03-CONTEXT.md
@.planning/phases/03-persistence/03-RESEARCH.md
@internal/metricslog/jsonl_writer.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Retention Cleanup</name>
  <files>
    - internal/metricspersist/db.go
  </files>
  <action>
    Add logic to prune old data.
    1. In `db.go`, implement `Cleanup(db *sql.DB, retentionDays int) (int64, error)`.
    2. The function should execute `DELETE FROM metrics WHERE created_at < datetime('now', '-7 days')`.
    3. Call this function during `InitDB` or `Migrate` sequence at startup.
    4. (Optional) Setup a ticker in the writer goroutine to run this once every 24 hours.
  </action>
  <verify>
    Manually insert a record with a timestamp from 10 days ago into the DB and run the cleanup function. Verify the record is deleted.
  </verify>
  <done>
    Records older than 7 days are automatically removed from the database.
  </done>
</task>

<task type="auto">
  <name>Task 2: Disable JSONL and Clean up</name>
  <files>
    - internal/metricsruntime/usage_plugin.go
    - internal/metricslog/jsonl_writer.go
    - internal/metricslog/types.go
    - cmd/server/main.go
  </files>
  <action>
    Remove the legacy JSONL logging path.
    1. Remove `metricslog.Enqueue` call from `internal/metricsruntime/usage_plugin.go`.
    2. Remove JSONL initialization/closing logic from `cmd/server/main.go`.
    3. Delete the `internal/metricslog` package files if no other dependencies exist.
    4. Update any remaining imports.
  </action>
  <verify>
    Run the server, make requests, and verify NO new `.jsonl` files are created in the `logs/` directory.
  </verify>
  <done>
    JSONL logging is completely removed and the code is clean.
  </done>
</task>

</tasks>

<verification>
Check `logs/` directory for any remaining or new `.jsonl` metrics files.
Verify DB size is stable after cleanup runs.
</verification>

<success_criteria>
1. Only metrics from the last 7 days exist in the DB.
2. `internal/metricslog` is gone or unused.
3. No JSONL metrics logs are generated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence/03-03-SUMMARY.md`
</output>

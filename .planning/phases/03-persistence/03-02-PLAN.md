---
phase: 03-persistence
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/metricspersist/writer.go
  - internal/metricspersist/types.go
  - internal/metricsruntime/usage_plugin.go
autonomous: true
must_haves:
  truths:
    - "Metrics are written to SQLite asynchronously without blocking the request"
    - "Duplicates with the same request_id are ignored (or handled via ON CONFLICT)"
    - "request_path is NOT stored in the database"
  artifacts:
    - path: "internal/metricspersist/writer.go"
      provides: "Async background worker for DB inserts"
    - path: "internal/metricspersist/types.go"
      provides: "Internal record representation for DB"
  key_links:
    - from: "internal/metricsruntime/MetricsPlugin"
      to: "internal/metricspersist.Enqueue"
      via: "HandleUsage call"
---

<objective>
Implement an asynchronous SQLite writer and integrate it into the MetricsPlugin.

Purpose: Offload database I/O to a background goroutine to ensure zero impact on request latency.
Output: Metrics records flowing from the plugin to the SQLite database.
</objective>

<execution_context>
@/home/adam/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-persistence/03-CONTEXT.md
@.planning/phases/03-persistence/03-RESEARCH.md
@internal/metricsruntime/usage_plugin.go
@internal/metricslog/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Async Writer</name>
  <files>
    - internal/metricspersist/types.go
    - internal/metricspersist/writer.go
  </files>
  <action>
    Create the async writing infrastructure.
    1. Define `MetricRecord` struct in `types.go` matching the schema from Plan 01 (excluding `request_path`).
    2. In `writer.go`, implement a global (or singleton) writer with a buffered channel (e.g., size 1024).
    3. Implement `StartWriter(db *sql.DB)` which runs a background goroutine.
    4. The worker should read from the channel and use `INSERT INTO metrics ... ON CONFLICT(request_id) DO NOTHING`.
    5. Implement `Enqueue(record MetricRecord)` which performs a non-blocking send to the channel.
  </action>
  <verify>
    Write a unit test in `writer_test.go` that enqueues multiple records and verifies they appear in a temporary SQLite database.
  </verify>
  <done>
    Records sent to `Enqueue` are persisted to the DB by the background worker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate MetricsPlugin</name>
  <files>
    - internal/metricsruntime/usage_plugin.go
  </files>
  <action>
    Update the `MetricsPlugin` to use the new SQLite writer.
    1. In `HandleUsage`, extract `request_id` using `logging.GetRequestID(ctx)`.
    2. Map the existing metrics data into a `metricspersist.MetricRecord`.
    3. Call `metricspersist.Enqueue(record)`.
    4. Keep the existing `metricslog.Enqueue(line)` for now (it will be removed in Plan 03).
    5. Ensure `request_path` is NOT passed to the DB record.
  </action>
  <verify>
    Run the server, make a request, and check `sqlite3 logs/metrics.db "SELECT * FROM metrics;"`.
  </verify>
  <done>
    Every AI request results in a corresponding row in the SQLite database.
  </done>
</task>

</tasks>

<verification>
Check database row count matches the number of successful requests made during testing.
Verify `request_id` in DB matches the `X-Request-ID` header if provided by the client.
</verification>

<success_criteria>
1. Metrics data persists to `logs/metrics.db`.
2. No performance regression in request processing.
3. No `request_path` stored in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence/03-02-SUMMARY.md`
</output>

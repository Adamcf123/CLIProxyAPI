---
phase: 03-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/metricspersist/db.go
  - internal/metricspersist/migrations.go
  - internal/metricspersist/migrations/0001_initial_schema.sql
  - cmd/server/main.go
autonomous: true
must_haves:
  truths:
    - "System fails to start if SQLite migration fails"
    - "SQLite database file is created at logs/metrics.db"
    - "Database schema includes required metrics fields and request_id unique constraint"
  artifacts:
    - path: "internal/metricspersist/db.go"
      provides: "SQLite connection management and PRAGMA configuration"
    - path: "internal/metricspersist/migrations/0001_initial_schema.sql"
      provides: "Table definition with metrics fields"
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/metricspersist.Migrate"
      via: "startup hook"
---

<objective>
Setup the core SQLite infrastructure with embedded migrations and fail-fast startup integration.

Purpose: Ensure the database is correctly provisioned and versioned before the service accepts traffic.
Output: Working DB connection management and schema migration system.
</objective>

<execution_context>
@/home/adam/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-persistence/03-CONTEXT.md
@.planning/phases/03-persistence/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create metricspersist package and migrations</name>
  <files>
    - internal/metricspersist/db.go
    - internal/metricspersist/migrations.go
    - internal/metricspersist/migrations/0001_initial_schema.sql
  </files>
  <action>
    Create a new internal package for persistence.
    1. Define `0001_initial_schema.sql` with a `metrics` table. Columns: `request_id` (TEXT PRIMARY KEY), `provider` (TEXT), `model` (TEXT), `tps` (REAL), `ttft` (REAL), `tpot` (REAL), `input_tokens` (INTEGER), `output_tokens` (INTEGER), `total_tokens` (INTEGER), `duration_ms` (INTEGER), `status_code` (INTEGER), `error_info` (TEXT), `created_at` (DATETIME).
    2. Implement `db.go` with `InitDB(path string) (*sql.DB, error)`. Use `modernc.org/sqlite`. Set PRAGMAs: `journal_mode=WAL`, `synchronous=NORMAL`, `busy_timeout=5000`.
    3. Implement `migrations.go` using `pressly/goose/v3` and `go:embed`. Provide `Migrate(db *sql.DB) error` function.
  </action>
  <verify>
    Run `go test ./internal/metricspersist/...` to verify DB initialization and migration success.
  </verify>
  <done>
    `logs/metrics.db` exists with correct table structure after `InitDB` and `Migrate` calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate into server startup</name>
  <files>
    - cmd/server/main.go
  </files>
  <action>
    Modify the server entry point to initialize the database before starting the HTTP server.
    1. In `main.go`, call `metricspersist.InitDB("logs/metrics.db")`.
    2. Immediately call `metricspersist.Migrate(db)`.
    3. If either fails, log the error and `os.Exit(1)`.
    4. Ensure the `logs/` directory is created if it doesn't exist.
  </action>
  <verify>
    Start the server with an invalid DB path (e.g. a directory) and verify it exits with error.
    Start normally and verify `logs/metrics.db` is created.
  </verify>
  <done>
    Server fails fast on migration error and creates DB on success.
  </done>
</task>

</tasks>

<verification>
Check if `sqlite3 logs/metrics.db ".schema"` shows the `metrics` table.
Verify server logs show successful migration or fatal error.
</verification>

<success_criteria>
1. `logs/metrics.db` is present.
2. `metrics` table exists with all required columns.
3. Server startup is blocked by migration failure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence/03-01-SUMMARY.md`
</output>

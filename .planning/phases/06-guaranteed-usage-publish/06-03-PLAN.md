---
phase: 06-guaranteed-usage-publish
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/runtime/executor/gemini_executor.go
  - internal/runtime/executor/gemini_cli_executor.go
  - internal/runtime/executor/gemini_vertex_executor.go
  - internal/runtime/executor/aistudio_executor.go
autonomous: true

must_haves:
  truths:
    - "Gemini/Vertex/AIStudio requests with missing usage still emit a usage record and become queryable in SQLite."
    - "Streaming paths call ensurePublished at stream end, even if no usage chunk appears."
    - "Failure outcomes are not overridden by ensurePublished."
  artifacts:
    - path: "internal/runtime/executor/gemini_executor.go"
      provides: "ensurePublished coverage for non-stream + stream"
    - path: "internal/runtime/executor/gemini_cli_executor.go"
      provides: "ensurePublished coverage for non-stream + stream"
    - path: "internal/runtime/executor/gemini_vertex_executor.go"
      provides: "ensurePublished coverage for non-stream + stream"
    - path: "internal/runtime/executor/aistudio_executor.go"
      provides: "ensurePublished coverage for non-stream + stream"
  key_links:
    - from: "internal/runtime/executor/*_executor.go"
      to: "(*usageReporter).ensurePublished"
      via: "defer in streaming goroutine or end-of-request"
      pattern: "ensurePublished\(ctx\)"
---

<objective>
Ensure Gemini/Vertex/AIStudio executors always publish at least one usage record per request, even when upstream omits usage/tokens.

Purpose: Close the no-usage persistence gap for Gemini-family providers.
Output: Executor wiring that guarantees an emitted usage record without weakening failure semantics.

Note: Some non-Gemini executors already have `ensurePublished` coverage; this plan is scoped to Gemini/Vertex/AIStudio paths that currently lack the guarantee.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-guaranteed-usage-publish/06-CONTEXT.md

@internal/runtime/executor/gemini_executor.go
@internal/runtime/executor/gemini_cli_executor.go
@internal/runtime/executor/gemini_vertex_executor.go
@internal/runtime/executor/aistudio_executor.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Non-streaming paths use reporter.finalize (no defer-order hazards)</name>
  <files>
internal/runtime/executor/gemini_executor.go
internal/runtime/executor/gemini_cli_executor.go
internal/runtime/executor/gemini_vertex_executor.go
internal/runtime/executor/aistudio_executor.go
  </files>
  <action>
For each non-streaming execution method that creates a `usageReporter`:

- Replace the existing `defer reporter.trackFailure(ctx, &err)` with:
  - `defer reporter.finalize(ctx, &err)`

Rationale:
- `finalize` is a single canonical end-of-request hook:
  - publishes failure if `err != nil`
  - then calls `ensurePublished` so no-usage successes still publish
- This removes a fragile dependency on defer ordering.

Behavioral intent:
- `reporter.publish(...)` skips when tokens are all zero and not failed.
- `reporter.finalize(...)` guarantees an emitted usage record exists for counting/persistence.
  </action>
  <verify>go test ./internal/runtime/executor -run Test -count=1</verify>
  <done>
In these executors, a successful request with missing usage still results in one published usage record.
  </done>
</task>

<task type="auto">
  <name>Task 2: Streaming goroutines ensure publish at stream end</name>
  <files>
internal/runtime/executor/gemini_executor.go
internal/runtime/executor/gemini_cli_executor.go
internal/runtime/executor/gemini_vertex_executor.go
internal/runtime/executor/aistudio_executor.go
  </files>
  <action>
For each streaming method that starts a goroutine to process a stream:

- Add `defer reporter.ensurePublished(ctx)` near the top of the goroutine.
- Keep existing failure calls (e.g., `reporter.publishFailure(ctx)`) unchanged.
- Ensure early returns inside the goroutine still trigger ensurePublished.

Important:
- Do NOT attempt to move ensurePublished outside the goroutine: `ExecuteStream` returns before the stream finishes.
  </action>
  <verify>go test ./internal/runtime/executor -run Test -count=1</verify>
  <done>
For streaming requests that emit no usage chunk, ensurePublished is still called at the end of the stream.
  </done>
</task>

</tasks>

<verification>
- `go test ./internal/runtime/executor` passes
</verification>

<success_criteria>
- Gemini/Vertex/AIStudio non-streaming requests no longer disappear when usage is missing
- Gemini/Vertex/AIStudio streaming requests always trigger ensurePublished at stream end
</success_criteria>

<output>
After completion, create `.planning/phases/06-guaranteed-usage-publish/06-03-SUMMARY.md`
</output>

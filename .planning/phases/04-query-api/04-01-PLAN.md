---
phase: 04-query-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/metricspersist/migrations/0002_add_streaming.sql
  - internal/metricspersist/types.go
  - internal/metricspersist/writer.go
  - internal/metricspersist/writer_test.go
  - internal/metricsruntime/usage_plugin.go
autonomous: true
must_haves:
  truths:
    - "SQLite metrics 表包含 streaming 维度，且默认值可用（旧数据至少可被查询/分组）"
    - "新写入的 metrics 行会写入 streaming 值（0/1），满足按 provider+model+streaming 分组的查询契约"
  artifacts:
    - path: "internal/metricspersist/migrations/0002_add_streaming.sql"
      provides: "metrics 表 streaming 列与必要索引"
      contains: "ADD COLUMN streaming"
    - path: "internal/metricspersist/types.go"
      provides: "MetricRecord 增加 Streaming 字段"
    - path: "internal/metricspersist/writer.go"
      provides: "SQLite INSERT 语句包含 streaming 列"
  key_links:
    - from: "internal/metricsruntime/usage_plugin.go"
      to: "internal/metricspersist.Enqueue"
      via: "MetricRecord.Streaming"
      pattern: "Streaming:.*snap\\.Streaming"
    - from: "internal/metricspersist/writer.go"
      to: "metrics.streaming"
      via: "INSERT INTO metrics"
      pattern: "INSERT INTO metrics[\\s\\S]*streaming"
---

<objective>
补齐持久化层的 streaming 维度，使 Query API 可以按 provider/model/streaming 过滤与分组。

Purpose: Phase 4 的锁定契约要求按 streaming 过滤/分组；当前 SQLite schema 缺失该列会阻断 STOR-02/03/04。
Output: 新 migration + 写入链路写入 streaming + 测试覆盖。
</objective>

<execution_context>
@/home/adam/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-query-api/04-CONTEXT.md
@.planning/phases/04-query-api/04-RESEARCH.md

@internal/metricspersist/migrations/0001_initial_schema.sql
@internal/metricspersist/types.go
@internal/metricspersist/writer.go
@internal/metricsruntime/usage_plugin.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 streaming 列（migration + 索引）</name>
  <files>
    - internal/metricspersist/migrations/0002_add_streaming.sql
  </files>
  <action>
    新增一个 goose migration（保持现有 `//go:embed migrations/*.sql` 的自动加载方式）。

    1) 创建 `internal/metricspersist/migrations/0002_add_streaming.sql`：
       - Up：
         - `ALTER TABLE metrics ADD COLUMN streaming INTEGER NOT NULL DEFAULT 0;`
           说明：历史数据无法可靠回填 streaming；用 DEFAULT 0 让查询契约可运行且不会产生 NULL 分组。
         - 添加至少一个索引以支持 Phase 4 的范围查询 + 分组：
           - 推荐：`CREATE INDEX IF NOT EXISTS idx_metrics_created_at ON metrics(created_at);`
           - 推荐：`CREATE INDEX IF NOT EXISTS idx_metrics_dim_time ON metrics(provider, model, streaming, created_at);`
       - Down：SQLite 对 DROP COLUMN 支持有限；Down 可以只 DROP INDEX（保守）或标注不可逆（按项目既有 goose 用法处理）。

    2) 注意不要改动 0001 初始 schema（迁移应是增量）。
  </action>
  <verify>
    运行 `go test ./internal/metricspersist/...` 并确认 migration 正常执行。
  </verify>
  <done>
    新建 DB 执行 `Migrate(db)` 后，`metrics` 表包含 `streaming` 列且相关索引存在。
  </done>
</task>

<task type="auto">
  <name>Task 2: 写入链路填充 streaming（MetricRecord + usage_plugin + writer）</name>
  <files>
    - internal/metricspersist/types.go
    - internal/metricsruntime/usage_plugin.go
    - internal/metricspersist/writer.go
    - internal/metricspersist/writer_test.go
  </files>
  <action>
    让每条 metrics 行携带 streaming 值，并由 writer 入库。

    1) 扩展 `internal/metricspersist/types.go`：
       - 在 `MetricRecord` 增加字段 `Streaming *bool`（nullable 以兼容未来非标准输入；但当前写入路径应始终赋值）。
         注意：SQLite 中用 INTEGER 0/1 存储；转换在 writer 层完成。

    2) 修改 `internal/metricsruntime/usage_plugin.go`：
       - 构造 `metricspersist.MetricRecord{...}` 时填充 `Streaming: &snap.Streaming`。
       - 不要引入额外的配置开关（locked decision 已固定维度）。

    3) 修改 `internal/metricspersist/writer.go`：
       - 在 `INSERT INTO metrics (...)` 列表中加入 `streaming`。
       - 在 VALUES 中加入对应占位符与参数。
       - 将 `*bool` 映射为 `int64`（true->1, false->0）；若指针为 nil，则写入 0（fail-closed，避免 NULL 分组）。

    4) 更新/增强 `internal/metricspersist/writer_test.go`：
       - Enqueue 至少一条 `Streaming=true` 与一条 `Streaming=false` 的记录。
       - 等待写入后，查询 `SELECT streaming FROM metrics WHERE request_id = ?` 并断言分别为 1/0。
  </action>
  <verify>
    运行 `go test ./...`。
  </verify>
  <done>
    新写入的 metrics 行在 DB 中具备正确的 `streaming` 值（0/1），且测试覆盖该行为。
  </done>
</task>

</tasks>

<verification>
1) `go test ./internal/metricspersist/...` 通过。
2) writer 测试能读回 `metrics.streaming` 并验证 0/1。
</verification>

<success_criteria>
1) DB schema 支持 streaming 过滤/分组。
2) 写入链路保证 streaming 维度不会缺失。
</success_criteria>

<output>
After completion, create `.planning/phases/04-query-api/04-01-SUMMARY.md`
</output>

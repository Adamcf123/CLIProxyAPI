---
phase: 02-metrics-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/metrics/collector.go
  - internal/metrics/collector_test.go
  - internal/metricsruntime/request_state.go
  - sdk/api/handlers/stream_forwarder.go
autonomous: true

must_haves:
  truths:
    - 流式转发链路（sdk/api/handlers/stream_forwarder.go:ForwardStream）能够在首个有效 chunk flush 时采集 TTFT 锚点（不把 keep-alive 当作首 token）
    - TPSCollector（internal/metrics/collector.go）在并发请求场景下不会因 windows map 读写触发 data race 或 panic
  artifacts:
    - path: internal/metricsruntime/request_state.go
      provides: 单请求指标状态对象（tracking_id、start_time、first_token_time 等）及 Gin context 绑定/读取
    - path: sdk/api/handlers/stream_forwarder.go
      provides: ForwardStream 在写出 chunk 后触发 TTFT 采样 hook
    - path: internal/metrics/collector.go
      provides: 并发安全的 TPSCollector（保护 windows map 的访问）
  key_links:
    - from: sdk/api/handlers/stream_forwarder.go
      to: internal/metricsruntime/request_state.go
      via: chunk flush 后调用 request state 的 MaybeRecordFirstToken
    - from: internal/metrics/collector.go
      to: internal/metrics/window.go
      via: windows map 的 value 为 SlidingWindow（已自带 RWMutex）
---

<objective>
把“首 token 采样点”和“collector 并发安全”打牢，为 Phase 2 后续的实时显示/汇总/落盘提供可复用的采集基础。

Purpose: 指标采集必须旁路且稳定，不能因为并发和 keep-alive 导致 TTFT 偏小或崩溃。
Output: request-scoped state + ForwardStream TTFT hook + 并发安全 TPSCollector。
</objective>

<execution_context>
@/home/adam/.claude/get-shit-done/workflows/execute-plan.md
@/home/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-metrics-collection/02-CONTEXT.md
@.planning/phases/02-metrics-collection/02-RESEARCH.md

关键挂钩点：`/home/adam/projects/CLIProxyAPI/sdk/api/handlers/stream_forwarder.go` 的 `ForwardStream`。
并发风险点：`/home/adam/projects/CLIProxyAPI/internal/metrics/collector.go` 的 `getOrCreateWindow` 注释明确“非并发安全”。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 让 TPSCollector 的 windows map 并发安全</name>
  <files>
/home/adam/projects/CLIProxyAPI/internal/metrics/collector.go
/home/adam/projects/CLIProxyAPI/internal/metrics/collector_test.go
  </files>
  <action>
在 TPSCollector（internal/metrics/collector.go）引入 `sync.RWMutex` 并保护对 `windows map[MetricKey]*SlidingWindow` 的访问：

- 写路径：`CompleteRequest(...)`、`getOrCreateWindow(...)` 创建/写入 map 时必须持写锁。
- 读路径：`GetWindowStats(...)`、`GetAllKeys()` 读取 map 时必须持读锁。
- 保持 SlidingWindow 的锁语义不变（window 本身已有 RWMutex）。
- 更新/补充单元测试以覆盖并发调用场景：并发 Start/Complete/GetStats 不应触发 data race（用 `go test -race` 验证）。

注意：该任务是架构性修复，目的是让 collector 在多请求并发时“设计上不可能出错”，避免在调用处散落锁/串行化逻辑。
  </action>
  <verify>go test -race ./internal/metrics -run Test -v</verify>
  <done>TPSCollector 在 -race 下无 data race；并发访问 windows map 不会 panic。</done>
</task>

<task type="auto">
  <name>Task 2: 建立单请求指标状态对象并挂钩 ForwardStream 的首 token 采样</name>
  <files>
/home/adam/projects/CLIProxyAPI/internal/metricsruntime/request_state.go
/home/adam/projects/CLIProxyAPI/sdk/api/handlers/stream_forwarder.go
  </files>
  <action>
新增 `internal/metricsruntime/request_state.go`（新 package，建议命名 `metricsruntime`），实现“单请求指标状态对象（per-request state）”用于跨采样点汇聚数据：

- 定义 `RequestState`（字段至少包含）：
  - `TrackingID`（string，uuid）
  - `StartedAt`（time.Time）
  - `FirstTokenAt`（*time.Time 或 time.Time + bool 标记，确保只记录一次）
  - `Streaming`（bool）
  - `RequestedModel`（string，可选）
  - `Provider`/`Model`（string，可选，后续由 usage plugin 填充）
- 提供 Gin context 绑定/读取 API（避免“黑洞参数”，不使用 map[string]any 乱塞）：
  - `AttachRequestState(c *gin.Context, state *RequestState)`
  - `GetRequestState(c *gin.Context) (*RequestState, bool)`
- 提供首 token 记录 helper：
  - `MaybeRecordFirstToken(c *gin.Context, chunk []byte, now time.Time)`
  - 过滤 keep-alive（`: keep-alive\n\n`）与空 chunk；只在“真正 payload chunk”写出后记录。

然后修改 `sdk/api/handlers/stream_forwarder.go` 的 `ForwardStream`：
- 在 `case chunk := <-data` 写出并 `flusher.Flush()` 后，调用 `metricsruntime.MaybeRecordFirstToken(c, chunk, time.Now())`。
- 不在 keep-alive 分支触发首 token。

目标：ForwardStream 成为 TTFT 的统一采样点，后续不需要在各 provider handler 里重复写采样逻辑。
  </action>
  <verify>go test ./... -run Test -v</verify>
  <done>ForwardStream 能在首个有效 chunk flush 后记录 FirstTokenAt，且 keep-alive 不会触发。</done>
</task>

</tasks>

<verification>
- `go test -race ./internal/metrics -v` 通过（验证 collector 并发安全）
- `go test ./... -v` 通过（确保 stream_forwarder 改动不破坏编译/测试）
</verification>

<success_criteria>
- TTFT 采样点统一落在 `ForwardStream` 的写出/flush 后，且排除 keep-alive
- TPSCollector windows map 在并发场景下无 data race
- request-scoped state 具备后续展示/落盘所需的最小字段与 API
</success_criteria>

<output>
After completion, create `/home/adam/projects/CLIProxyAPI/.planning/phases/02-metrics-collection/02-01-SUMMARY.md`
</output>

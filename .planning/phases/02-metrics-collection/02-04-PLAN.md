---
phase: 02-metrics-collection
plan: 04
type: execute
gap_closure: true
wave: 1
depends_on: [02-01, 02-02]
files_modified:
  - sdk/api/handlers/stream_forwarder.go
  - sdk/api/handlers/openai/openai_handlers.go
  - sdk/api/handlers/gemini/gemini_handlers.go
  - sdk/api/handlers/claude/code_handlers.go
autonomous: true

must_haves:
  truths:
    - 所有流式 provider（OpenAI/Gemini/Claude）的“首个内容 token”（用户可见文本）会触发 TTFT 采样，TTFT 不会因“首 chunk 绕过 ForwardStream”或“首 chunk 仅包含元数据/role”而失真
    - Gemini/Claude 在发起上游请求之前就完成 RequestState 绑定（AttachRequestState），确保 TTFT 的开始时间从请求真正开始计算
    - 采样路径单一：所有实际写出到 client 的 payload chunk 都经由同一条 ForwardStream 写出/flush，从而统一触发 metricsruntime.MaybeRecordFirstContentToken
  artifacts:
    - path: sdk/api/handlers/stream_forwarder.go
      provides: 统一的流式转发写出路径（包含首 chunk 在内的 TTFT 采样点）
    - path: sdk/api/handlers/openai/openai_handlers.go
      provides: OpenAI streaming 不再手动写出/flush 首 chunk；改为 prefetch 后统一交给 ForwardStream
    - path: sdk/api/handlers/gemini/gemini_handlers.go
      provides: Gemini streaming 在任何 chunk 写出前绑定 RequestState，且首 chunk 也经由 ForwardStream
    - path: sdk/api/handlers/claude/code_handlers.go
      provides: Claude streaming 在任何 chunk 写出前绑定 RequestState，且首 chunk 也经由 ForwardStream
  key_links:
    - from: sdk/api/handlers/*/*handlers.go
      to: internal/metricsruntime/request_state.go
      via: metricsruntime.AttachRequestState 在第一次写出前完成
    - from: sdk/api/handlers/stream_forwarder.go
      to: internal/metricsruntime/request_state.go
      via: ForwardStream 在写出并 Flush 后调用 metricsruntime.MaybeRecordFirstContentToken
---

<objective>
把“用户看到的本次请求指标汇总”变成可信的：无论 OpenAI/Gemini/Claude，TTFT 都以真实首个有效 payload chunk 为锚点采样，避免 TTFT 缺失/偏大导致汇总里 TTFT/TPOT/TPS 不可靠。

Purpose: Phase 2 的交付不仅要“能输出 metrics_summary”，还要保证其关键字段（TTFT/TPOT/TPS）在所有 provider 上语义一致。
Output: provider handler 的首 chunk 行为收敛到 ForwardStream 的单一路径；Gemini/Claude 的 state 绑定时机前移到写出之前。
</objective>

<execution_context>
@/home/adam/.claude/get-shit-done/workflows/execute-plan.md
@/home/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-metrics-collection/02-VERIFICATION.md
@.planning/phases/02-metrics-collection/02-02-SUMMARY.md

已建立的采样契约（02-01）：TTFT 在 ForwardStream 的“首个内容 token 写出并 flush 后”记录（排除 keep-alive 与仅元数据/role chunk）。
当前阻塞项：多个 provider 在进入 ForwardStream 之前先手动写出/flush 首 chunk，且 Gemini/Claude 甚至在首 chunk 之后才 AttachRequestState，导致 TTFT 无法覆盖真实首 token。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立“首 chunk 也走 ForwardStream”的统一转发入口</name>
  <files>
/home/adam/projects/CLIProxyAPI/sdk/api/handlers/stream_forwarder.go
  </files>
  <action>
目标：让所有“写出到 client 的 payload chunk（包括第一个）”都必须通过 ForwardStream 的写出/Flush 路径，从而保证 TTFT 采样点不被绕过。

建议实现方式（选其一，优先 A）：

A) 在 forwarder 层提供“prefetch + 正式转发”的单一路径 API（不新增业务开关参数）：
- 新增一个面向 handler 的 helper（例如 `ForwardStreamWithPrefetch(...)` / `ForwardStreamPrefetched(...)`）：
  - 输入：gin writer/flusher、upstream reader、以及“已预读的首个 payload chunk（可能为空）”
  - 行为：如果存在 prefetched chunk，先通过 forwarder 统一 write + Flush（而不是让 handler 手写 write/Flush），并在 flush 后触发 `metricsruntime.MaybeRecordFirstToken`；随后继续按现有 ForwardStream 逻辑转发剩余 chunk。
  - 约束：保持 02-01 的 keep-alive 过滤策略一致（prefetch 也要经过同样的有效性判定）。

B) 如果必须保留 handler 的首 chunk write/Flush（不推荐）：
- 明确在“首 chunk flush 的同一位置”调用 `metricsruntime.MaybeRecordFirstToken`，且强制要求 state 已 Attach（由 handler 保证）。

验收点：forwarder API 的语义清晰，调用方不再需要手动 write/Flush 首 chunk；TTFT hook 覆盖 prefetched chunk。
  </action>
  <verify>go test ./... -run Test -v</verify>
  <done>forwarder 层具备可复用的 prefetch 转发入口，能把首 chunk 纳入 TTFT 采样路径。</done>
</task>

<task type="auto">
  <name>Task 2: OpenAI streaming 首 chunk 不再绕过 ForwardStream</name>
  <files>
/home/adam/projects/CLIProxyAPI/sdk/api/handlers/openai/openai_handlers.go
/home/adam/projects/CLIProxyAPI/sdk/api/handlers/stream_forwarder.go
  </files>
  <action>
现状问题（来自 02-VERIFICATION）：`handleStreamingResponse` 先写出/flush 首 chunk，再把剩余 chunk 交给 ForwardStream，导致首 chunk 不触发 TTFT hook。

目标：OpenAI streaming 即使需要“先 peek 首段数据用于决定 headers/错误处理”，也不直接写出首 chunk；而是把该 chunk 作为 prefetched 输入交给 forwarder，确保 TTFT hook 覆盖。

实施要点：
- 保留“prefetch 首 chunk 用于判定/修正响应头、识别错误”的业务意图，但把“写出”动作延后到 forwarder。
- 使用 `io.MultiReader(bytes.NewReader(firstChunk), resp.Body)` 或 forwarder 的 prefetched 接口保证转发数据不丢失。
- 明确在任何 write/Flush 发生之前，RequestState 已 Attach（OpenAI 当前已满足，但需要确保 prefetch 分支也不例外）。

验收点：OpenAI streaming 路径中不再存在“绕过 forwarder 的首 chunk 写出/flush”；TTFT 在真实首 payload chunk flush 后被记录。
  </action>
  <verify>go test ./... -run Test -v</verify>
  <done>OpenAI 的首 chunk 与后续 chunk 走同一条 forwarder 写出路径，TTFT 采样稳定。</done>
</task>

<task type="auto">
  <name>Task 3: Gemini streaming 在写出任何 chunk 之前 AttachRequestState，并让首 chunk 走 ForwardStream</name>
  <files>
/home/adam/projects/CLIProxyAPI/sdk/api/handlers/gemini/gemini_handlers.go
/home/adam/projects/CLIProxyAPI/sdk/api/handlers/stream_forwarder.go
  </files>
  <action>
现状问题（来自 02-VERIFICATION）：Gemini 先写出/flush 首 chunk，之后才 `NewRequestState + AttachRequestState`，因此 TTFT 永远不可能代表真实首 token。

目标：把 RequestState 的创建/绑定前移到任何 chunk 写出之前；并把 prefetch 的首 chunk 也交给 forwarder 统一写出。

实施要点：
- 在进入任何“写响应”分支之前完成：
  - `state := metricsruntime.NewRequestState(...)`
  - `metricsruntime.AttachRequestState(c, state)`
  - `stop := metricsruntime.StartLiveDisplay(state)` + `defer stop()`
- 如需 prefetch 首 chunk（用于识别错误/补 headers）：只读取不写出；将该 chunk 作为 prefetched 输入传给 forwarder。

验收点：Gemini handler 中不会出现“state 绑定发生在首 chunk flush 之后”；首 chunk 也触发 TTFT hook。
  </action>
  <verify>go test ./... -run Test -v</verify>
  <done>Gemini streaming 的 TTFT 采样覆盖真实首 token；与 OpenAI/Claude 的采样语义一致。</done>
</task>

<task type="auto">
  <name>Task 4: Claude streaming 在写出任何 chunk 之前 AttachRequestState，并让首 chunk 走 ForwardStream</name>
  <files>
/home/adam/projects/CLIProxyAPI/sdk/api/handlers/claude/code_handlers.go
/home/adam/projects/CLIProxyAPI/sdk/api/handlers/stream_forwarder.go
  </files>
  <action>
现状问题（来自 02-VERIFICATION）：Claude 先写出/flush 首 chunk，之后才 `NewRequestState + AttachRequestState`，导致 TTFT 无法覆盖真实首 token。

目标：与 Gemini 同步收敛：state 绑定先于任何写出；首 chunk 通过 forwarder。

实施要点：
- 把 `NewRequestState/AttachRequestState/StartLiveDisplay` 前移到首个 write/Flush 之前。
- 若 Claude handler 需要 prefetch 首 chunk：只读不写，交给 forwarder 的 prefetched 接口写出。

验收点：Claude streaming 的首 payload chunk flush 能触发 TTFT 记录；跨 provider 指标收集链路一致。
  </action>
  <verify>go test ./... -run Test -v</verify>
  <done>Claude streaming 的 TTFT 采样覆盖真实首 token；指标汇总里 TTFT/TPOT/TPS 不再因首 chunk 绕过而缺失/偏大。</done>
</task>

</tasks>

<verification>
- `go test ./... -v` 通过
- 人工验证（必须）：分别发起 OpenAI/Gemini/Claude 各 1 次 streaming 请求
  - 期望：stderr 仅输出 1 行 `metrics_summary {json}`；当 usage 可用时 ttft/tpot/tps 为数值
  - 关键点：TTFT 不应为 null，且不应明显偏大（不再受“首 chunk 先写出”影响）
</verification>

<success_criteria>
- OpenAI/Gemini/Claude：首个实际写出的 payload chunk 必定触发 TTFT 采样
- Gemini/Claude：AttachRequestState 发生在任何写出之前
- 采样路径单一且可复用：handler 不再直接 write/Flush 首 chunk 来规避 forwarder
</success_criteria>

<output>
After completion, create `/home/adam/projects/CLIProxyAPI/.planning/phases/02-metrics-collection/02-04-SUMMARY.md`
</output>

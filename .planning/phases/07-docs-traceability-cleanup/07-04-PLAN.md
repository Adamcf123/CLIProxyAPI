---
phase: 07-docs-traceability-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/sdk-access.md
  - docs/sdk-access_CN.md
  - docs/sdk-advanced.md
  - docs/sdk-advanced_CN.md
  - docs/sdk-usage.md
  - docs/sdk-usage_CN.md
  - docs/sdk-watcher.md
  - docs/sdk-watcher_CN.md
  - .planning/phases/07-docs-traceability-cleanup/07-04-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "本计划覆盖的 8 个 `docs/sdk-*.md` 文档不再出现将指标数据源描述为 JSONL 的表述"
    - "如果这些文档提及 metrics 持久化/落库位置，则必须明确指向 SQLite `logs/metrics.db`（或 `/v0/management/metrics` 查询入口）"
  artifacts:
    - path: "docs/sdk-usage.md"
      provides: "operator-facing SDK usage guide"
      contains: "sdk/cliproxy"
    - path: "docs/sdk-usage_CN.md"
      provides: "operator-facing SDK usage guide (CN)"
      contains: "sdk/cliproxy"
    - path: "docs/sdk-access.md"
      provides: "operator-facing SDK access guide"
      contains: "sdk/cliproxy"
    - path: "docs/sdk-access_CN.md"
      provides: "operator-facing SDK access guide (CN)"
      contains: "sdk/cliproxy"
    - path: "docs/sdk-advanced.md"
      provides: "operator-facing SDK advanced guide"
      contains: "sdk/cliproxy"
    - path: "docs/sdk-advanced_CN.md"
      provides: "operator-facing SDK advanced guide (CN)"
      contains: "sdk/cliproxy"
    - path: "docs/sdk-watcher.md"
      provides: "operator-facing SDK watcher guide"
      contains: "sdk/cliproxy"
    - path: "docs/sdk-watcher_CN.md"
      provides: "operator-facing SDK watcher guide (CN)"
      contains: "sdk/cliproxy"
  key_links:
    - from: "docs/sdk-*.md"
      to: "(no jsonl references)"
      via: "docs cleanup"
      pattern: "\\.jsonl|\\bJSONL\\b|metrics-YYYY"
---

<objective>
对 `docs/*.md` 做一次面向运维/使用者的文本一致性清理：确保不会把指标数据源误描述为 JSONL（当前单一来源是 SQLite `logs/metrics.db`）。

Purpose: 清除 operator-facing 文档中的遗留/误导性表述，降低排障与发布风险
Output: 清理后的 `docs/*.md` + 审计可复核 SUMMARY
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-docs-traceability-cleanup/07-CONTEXT.md
@docs/sdk-access.md
@docs/sdk-usage.md
@docs/sdk-advanced.md
@docs/sdk-watcher.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扫描并修正 docs/*.md 中遗留的 JSONL 数据源表述</name>
  <files>docs/sdk-access.md
docs/sdk-access_CN.md
docs/sdk-advanced.md
docs/sdk-advanced_CN.md
docs/sdk-usage.md
docs/sdk-usage_CN.md
docs/sdk-watcher.md
docs/sdk-watcher_CN.md</files>
  <action>
在本计划覆盖的 8 个 `docs/sdk-*.md` 文件中定位任何 `.jsonl` / `JSONL` / `metrics-YYYY` 等表述：

- 如果出现上述关键词：统一删除/替换这些关键词（即使是 legacy 描述也不保留术语），避免读者和 grep 工具把它当成当前事实来源。
- 可用不包含 `jsonl/JSONL` 字样的替代描述，例如："legacy metrics logs (decommissioned)"。
- 不引入新的行为承诺；只做文案与示例路径修正。
  </action>
  <verify>
python - <<'PY'
import pathlib
import sys

needles = ['.jsonl', 'jsonl', 'metrics-yyyy']
persist_signals = ['persist', 'persistence', 'storage', 'database', 'sqlite', '落库', '落盘', '数据库']
metrics_signals = ['metrics', 'tps', 'ttft', 'tpot', '指标']
must_point_to = ['logs/metrics.db', 'v0/management/metrics']
paths = [
    pathlib.Path('docs/sdk-access.md'),
    pathlib.Path('docs/sdk-access_CN.md'),
    pathlib.Path('docs/sdk-advanced.md'),
    pathlib.Path('docs/sdk-advanced_CN.md'),
    pathlib.Path('docs/sdk-usage.md'),
    pathlib.Path('docs/sdk-usage_CN.md'),
    pathlib.Path('docs/sdk-watcher.md'),
    pathlib.Path('docs/sdk-watcher_CN.md'),
]

for p in paths:
    if not p.exists():
        print(f"missing expected docs file: {p}")
        sys.exit(1)

    text = p.read_text(encoding='utf-8').lower()
    for n in needles:
        if n in text:
            print(f"unexpected '{n}' mention in {p}")
            sys.exit(1)

    # Only enforce SQLite pointer if the doc discusses *metrics persistence/storage*.
    has_metrics = any(s in text for s in metrics_signals)
    has_persist = any(s in text for s in persist_signals)
    if has_metrics and has_persist:
        if not any(m in text for m in must_point_to):
            print(f"{p} mentions metrics persistence/storage but does not reference logs/metrics.db or /v0/management/metrics")
            sys.exit(1)

print('ok')
PY
  </verify>
  <done>
本计划覆盖的 8 个 `docs/sdk-*.md` 文件中不再出现将指标数据源描述为 JSONL 的表述。
  </done>
</task>

<task type="auto">
  <name>Task 2: 生成 07-04 执行摘要（供审计复核）</name>
  <files>.planning/phases/07-docs-traceability-cleanup/07-04-SUMMARY.md</files>
  <action>
按 `~/.config/opencode/get-shit-done/templates/summary.md` 的风格生成 summary，至少包含：

- What changed: 具体修改了哪些 `docs/*.md`
- Why: 防止 operator docs 误导到 JSONL 数据源
- Verify: 记录本 plan 实际执行过的 python 校验（可原样粘贴）
  </action>
  <verify>
rg -nS "Phase 7" .planning/phases/07-docs-traceability-cleanup/07-04-SUMMARY.md
rg -nS "docs/" .planning/phases/07-docs-traceability-cleanup/07-04-SUMMARY.md
  </verify>
  <done>
外部审计读 `07-04-SUMMARY.md` 即可复核：docs/*.md 的 JSONL 遗留表述已清理。
  </done>
</task>

</tasks>

<verification>
- 本计划覆盖的 8 个 `docs/sdk-*.md` 文件不出现 `.jsonl`/`JSONL`/`metrics-YYYY`。
</verification>

<success_criteria>
- operator-facing docs 不会再把指标数据源误描述为 JSONL。
</success_criteria>

<output>
After completion, create `.planning/phases/07-docs-traceability-cleanup/07-04-SUMMARY.md`
</output>

---
phase: 07-docs-traceability-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - 重要命令.txt
  - .planning/phases/07-docs-traceability-cleanup/07-03-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "planning docs（`.planning/STATE.md`）关于 TPS 指标持久化的描述与当前实现一致：SQLite `logs/metrics.db` 为单一来源"
    - "operator docs（`重要命令.txt`）不再出现将指标数据源描述为 JSONL 的表述"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "current project state / decisions"
      contains: "logs/metrics.db"
    - path: "重要命令.txt"
      provides: "operator command snippets"
      contains: "metrics.db"
  key_links:
    - from: ".planning/STATE.md"
      to: "logs/metrics.db"
      via: "state decisions"
      pattern: "logs/metrics\\.db"
---

<objective>
修复 planning docs 与 operator docs 的“指标数据源”事实漂移，避免出现“同一仓库同时声称 JSONL 与 SQLite”为当前数据源的自相矛盾。

Purpose: 关闭审计指出的 docs drift 技术债，让发布/运维依据的文本与已验证实现一致
Output: 更新后的 `.planning/STATE.md` + operator docs 清理 + 审计可复核 SUMMARY
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-docs-traceability-cleanup/07-CONTEXT.md

@cmd/server/main.go
@internal/api/handlers/management/handler.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 STATE.md 中关于指标落盘位置的事实漂移</name>
  <files>.planning/STATE.md</files>
  <action>
在 `.planning/STATE.md` 的 “From Phase 02-metrics-collection” 段落中，修复与当前实现不一致的描述：

- 将“写入 logs/metrics-YYYY-MM-DD.jsonl / persist to JSONL”的描述改写为当前事实：指标通过 MetricsPlugin 异步写入 SQLite（默认 `logs/metrics.db`）。
- 将任何“JSONL 是当前数据源”的表述移除；`STATE.md` 不应再出现 `.jsonl`/`JSONL` 字样，以避免误导。
- 保持其余决策记录不变；只修复这一处“事实漂移/自相矛盾”。
  </action>
  <verify>
python - <<'PY'
import pathlib
import sys

path = pathlib.Path('.planning/STATE.md')
text = path.read_text(encoding='utf-8')
low = text.lower()

if 'logs/metrics.db' not in text:
    print('STATE.md must mention logs/metrics.db')
    sys.exit(1)

if '.jsonl' in low or 'jsonl' in low:
    print('STATE.md should not mention jsonl after cleanup')
    sys.exit(1)

print('ok')
PY
  </verify>
  <done>
`STATE.md` 对“指标持久化数据源”的描述与 Phase 03+ 的 SQLite 单一来源保持一致；不存在自相矛盾的历史残留。
  </done>
</task>

<task type="auto">
  <name>Task 2: 清理 重要命令.txt 中遗留的 JSONL 数据源表述</name>
  <files>重要命令.txt</files>
  <action>
对 operator-facing 文档做一次“事实漂移”清理：

- 扫描 `重要命令.txt`，定位任何 `.jsonl` / `JSONL` / `metrics-YYYY` 等“JSONL 仍为当前指标数据源”的表述。
- 若发现：将其改写为 SQLite 单一来源（`logs/metrics.db`），并在需要时引导到 `GET /v0/management/metrics` 查询入口。
- 不新增新的行为承诺；只修正文案与示例路径。
  </action>
  <verify>
python - <<'PY'
import pathlib
import sys

needles = ['.jsonl', 'jsonl', 'metrics-yyyy']

cmds = pathlib.Path('重要命令.txt')
if not cmds.exists():
    print('重要命令.txt missing')
    sys.exit(1)

text = cmds.read_text(encoding='utf-8').lower()
for n in needles:
    if n in text:
        print(f"unexpected '{n}' mention in {cmds}")
        sys.exit(1)

if 'metrics.db' not in text:
    print('重要命令.txt should mention metrics.db')
    sys.exit(1)

print('ok')
PY
  </verify>
  <done>
`重要命令.txt` 中不再出现将指标数据源描述为 JSONL 的表述，且保留/明确 `metrics.db` 指引。
  </done>
</task>

<task type="auto">
  <name>Task 3: 生成 07-03 执行摘要（供审计复核）</name>
  <files>.planning/phases/07-docs-traceability-cleanup/07-03-SUMMARY.md</files>
  <action>
按 `~/.config/opencode/get-shit-done/templates/summary.md` 的风格生成 summary，至少包含：

- What changed: `.planning/STATE.md` 的两处修复点；若有，列出被修订的 operator docs 文件
- Why: 关闭 docs drift（避免 JSONL/SQLite 自相矛盾），降低运维误判数据源风险
- Evidence: 引用 `cmd/server/main.go`/`internal/api/handlers/management/handler.go` 中 `logs/metrics.db` 与 `/v0/management/metrics` 的事实来源
- Verify: 记录本 plan 实际执行过的 python 校验（可原样粘贴）
  </action>
  <verify>
rg -nS "Phase 7" .planning/phases/07-docs-traceability-cleanup/07-03-SUMMARY.md
rg -nS "STATE\\.md" .planning/phases/07-docs-traceability-cleanup/07-03-SUMMARY.md
rg -nS "logs/metrics\\.db|v0/management/metrics" .planning/phases/07-docs-traceability-cleanup/07-03-SUMMARY.md
  </verify>
  <done>
外部审计读 `07-03-SUMMARY.md` 即可复核：planning/operator docs 的数据源描述已对齐为 SQLite。
  </done>
</task>

</tasks>

<verification>
- `.planning/STATE.md` 不再出现 `.jsonl`/`JSONL`；并明确 `logs/metrics.db`。
- `重要命令.txt` 不再出现将指标数据源描述为 JSONL 的表述。
</verification>

<success_criteria>
- 任何读 `STATE.md` / operator docs 的人都不会再被误导去找 JSONL 指标文件。
- Phase 7 的 docs drift 技术债（关于 JSONL vs SQLite 的冲突叙述）被彻底消除。
</success_criteria>

<output>
After completion, create `.planning/phases/07-docs-traceability-cleanup/07-03-SUMMARY.md`
</output>

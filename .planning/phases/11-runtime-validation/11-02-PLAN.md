---
phase: 11-runtime-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - .planning/phases/11-runtime-validation/11-RUNTIME-VALIDATION-REPORT.md
autonomous: false
user_setup:
  - service: local-runtime
    why: "调用 management endpoints 与本地验证运行"
    env_vars:
      - name: MANAGEMENT_KEY
        source: "Validation-only plaintext management key (may be written into artifacts-only temp config; must not be committed)"
      - name: API_KEY
        source: "Local client API key (default in config.yaml: sk-dummy)"

must_haves:
  truths:
    - "在真实-ish 负载下验证 metrics collection 不阻塞请求路径（无明显吞吐/延迟退化）"
    - "stderr 的 metrics_summary 与 SQLite 落库在目标运行方式下可用（可复现、可对照）"
    - "必须覆盖并记录结论：terminal error after headers committed / client cancel (499) / no-usage ensurePublished / persistence degraded observability"
  artifacts:
    - path: ".planning/phases/11-runtime-validation/11-RUNTIME-VALIDATION-REPORT.md"
      provides: "Audit-ready runtime validation report with evidence links"
      contains:
        - "Commands executed"
        - "Baseline run results"
        - "Edge case results (>=3 repetitions each)"
        - "Pass/fail conclusion"
  key_links:
    - from: ".planning/phases/11-runtime-validation/11-RUNTIME-VALIDATION-REPORT.md"
      to: ".planning/phases/11-runtime-validation/artifacts/"
      via: "evidence paths"
      note: "报告必须引用证据文件路径（stdout/stderr/resources/sqlite queries），而不是口头描述"
---

<objective>
执行 Phase 11 的 runtime validation：跑 baseline（轻负载 steady-state）+ 跑所有 required edge scenarios（每个至少 3 次），并产出可审计复核的 Markdown 报告。

Purpose: 这是发布前信心的最后一环，验证那些难以单测覆盖的流式边界语义与实际运行环境行为。
Output: `.planning/phases/11-runtime-validation/11-RUNTIME-VALIDATION-REPORT.md` + `.planning/phases/11-runtime-validation/artifacts/` 证据产物。
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-runtime-validation/11-CONTEXT.md
@.planning/phases/11-runtime-validation/11-REPORT-TEMPLATE.md
@.planning/phases/11-runtime-validation/scripts/run_baseline.sh
@.planning/phases/11-runtime-validation/scripts/run_edge_cases.sh
@重要命令.txt

## Phase 11 Guardrails (from context)

- 并发/速率控制优先（blast radius control）
- baseline mix: 50/50 streaming vs non-streaming
- 覆盖 2-3 个 mainstream provider/model（baseline run）
  - edge scenarios 每个至少 3 次，并写入报告

  ## Correlation key

  - 报告与脚本使用单一关联键 `request_id`。
  - 在本 phase 中，`request_id` 的来源为 server stderr `metrics_summary` JSON 的 `tracking_id` 字段（两者视为同一值）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 baseline（真实 providers，轻负载 steady-state）并采集证据</name>
  <files>.planning/phases/11-runtime-validation/11-RUNTIME-VALIDATION-REPORT.md</files>
  <action>
    1) 运行 baseline 脚本，使用 context 约束：并发 1-5，steady-state，50/50 streaming vs non-streaming。
       - 选择 2-3 个 mainstream provider/model 组合（必须在报告中列出）。
       - 如果某 provider 因本机未配置凭据而不可用，必须在报告中记录“跳过原因”并用可用 provider 替代，保证至少 2 个 provider 覆盖。

    2) 定义并记录明确的通过/未通过阈值（比较式阈值，允许 planner discretion）：
       - 资源证据（最重要）：RSS 增幅、CPU 峰值/稳态、goroutine/FD（若脚本能采集）
       - 请求侧证据：time_total 的 p50/p95（或最少 min/median/max）
       - 持久化健康：baseline run 期间 management meta.persistence 不应 degraded（或 degraded 必须可解释）

    3) 把 baseline run 产生的 evidence 文件路径写入报告（不要复制整段日志）。
       - 必须包含：run_dir、server stdout/stderr 路径、resource 采样文件、sqlite3 查询输出文件、curl timing 结果文件。

    4) 用 sqlite3 证明“落库可用”：
       - `logs/metrics.db` 存在
       - baseline 前后 metrics 行数有增长
       - 抽样展示最近 5 行（request_id/provider/model/streaming/status_code/error_info）

    报告文件请以模板为骨架：复制 `11-REPORT-TEMPLATE.md` → 填写并另存为 `11-RUNTIME-VALIDATION-REPORT.md`。
  </action>
  <verify>
    bash .planning/phases/11-runtime-validation/scripts/run_baseline.sh
  </verify>
  <done>
    - baseline run 可重复执行且产物落在 artifacts 下
    - 报告包含 baseline 的命令、阈值、结论与 evidence 路径
    - sqlite3 证据显示 metrics 行确实写入
  </done>
</task>

<task type="auto">
  <name>Task 2: 运行 required edge scenarios（每个 >=3 次）并记录结论</name>
  <files>.planning/phases/11-runtime-validation/11-RUNTIME-VALIDATION-REPORT.md</files>
  <action>
    运行 edge-case 脚本，确保覆盖 Phase 11 Context 列出的全部 required scenarios：

    - terminal error after headers committed
    - client cancel/disconnect (HTTP 499)
    - upstream no-usage path（ensurePublished 仍产生 DB 行）
    - persistence degraded path（queue_full / insert_failure 等）并验证 observability

    具体要求：
    1) 每个 scenario 至少重复 3 次，并把每次运行的 request_id、预期/实际、证据文件写入报告中的表格。
    2) primary observation point 必须是 SQLite：每次都有可查询行，并能从字段证明语义（status_code/error_info/tokens nullability 等）。
    3) 对 persistence degraded：必须在 management endpoint 中观察到 meta.persistence.degraded（并记录 last_drop_reason / dropped_total 等最小字段）。

    注意：edge-case 运行允许使用 mock upstream / 临时 config（仅在 artifacts 下），以确保可重复且不触发真实计费。
  </action>
  <verify>
    bash .planning/phases/11-runtime-validation/scripts/run_edge_cases.sh all
  </verify>
  <done>
    - 报告中每个 required scenario 都有 >=3 次运行记录与结论
    - 每次运行都有对应 SQLite 行可查
    - persistence degraded 的 observability 在 management meta 中可见且可解释
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 11 runtime validation 报告（baseline + edge scenarios）</what-built>
  <how-to-verify>
    1) 打开 `.planning/phases/11-runtime-validation/11-RUNTIME-VALIDATION-REPORT.md`
    2) 确认包含 baseline 的阈值与结论，并引用 artifacts 证据路径
    3) 确认 4 个 required edge scenarios 全部覆盖且每个 >=3 次
    4) 确认报告不包含任何 secrets（API keys、tokens、Authorization header 原文）
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- baseline 脚本与 edge-case 脚本均执行成功
- `sqlite3 <run_dir>/logs/metrics.db` 可查询到对应 request_id 行
- `curl -H "X-Management-Key: $MANAGEMENT_KEY" .../v0/management/metrics` 可在 degraded 场景观察到 meta.persistence
</verification>

<success_criteria>
- `.planning/phases/11-runtime-validation/11-RUNTIME-VALIDATION-REPORT.md` 存在且可审计复核
- phase goal 的 3 条 success criteria 在报告中有明确结论与证据支撑
</success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-validation/11-02-SUMMARY.md`
</output>

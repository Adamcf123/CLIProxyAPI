---
phase: 10-request-id-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/logging/requestid.go
  - internal/logging/requestid_test.go
autonomous: true

must_haves:
  truths:
    - "GenerateRequestID() 生成 16-character hex (64-bit) 而非 8-character (32-bit)"
    - "现有 8-char request_id 查询仍能正常工作（向后兼容）"
    - "request_id 冲突概率显著降低（从 2^32 空间提升到 2^64 空间）"
  artifacts:
    - path: "internal/logging/requestid.go"
      provides: "64-bit request ID generation"
      changes:
        - "GenerateRequestID() 返回 16-char hex"
        - "保持 crypto/rand 作为熵源"
    - path: "internal/logging/requestid_test.go"
      provides: "Request ID generation tests"
      contains:
        - "验证生成长度为 16 的 hex 字符串"
        - "验证字符集为 [0-9a-f]"
        - "验证多线程并发安全性"
  key_links:
    - from: "GenerateRequestID()"
      to: "所有使用 request_id 的组件"
      via: "函数调用"
      note: "保持 API 签名不变，仅改变内部实现"
---

<objective>
将 request_id 生成器从 32-bit (8-char hex) 升级到 64-bit (16-char hex)，显著降低碰撞概率。

Purpose: 当前 32-bit request_id 在 metrics 表作为主键，配合 ON CONFLICT DO NOTHING 去重；碰撞会导致"静默缺行"。升级到 64-bit 将碰撞概率降至可忽略水平。
Output: 修改后的 requestid.go 和对应的单元测试
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-request-id-robustness/10-CONTEXT.md

## Current Implementation

File: internal/logging/requestid.go
```go
// GenerateRequestID creates a new 8-character hex request ID.
func GenerateRequestID() string {
	b := make([]byte, 4)  // 32-bit = 4 bytes
	if _, err := rand.Read(b); err != nil {
		return "00000000"
	}
	return hex.EncodeToString(b)  // 4 bytes -> 8 hex chars
}
```

## Requirements

- 保持 hex 格式便于阅读
- 增加长度到 16 chars (64-bit = 8 bytes)
- 保持向后兼容：现有 8-char ID 在系统中继续有效
- 使用 crypto/rand 保证随机性
</context>

<tasks>

<task type="auto">
  <name>Task 1: 升级 GenerateRequestID 到 64-bit</name>
  <files>internal/logging/requestid.go</files>
  <action>
修改 GenerateRequestID() 函数：
1. 将字节数组从 4 bytes 改为 8 bytes: `b := make([]byte, 8)`
2. 更新 fallback 返回值从 "00000000" 到 "0000000000000000" (16 zeros)
3. 更新函数注释，说明生成 16-character hex request ID
4. 保持 crypto/rand 作为熵源
5. 保持函数签名不变：`func GenerateRequestID() string`

注意：不要修改其他函数（WithRequestID, GetRequestID, SetGinRequestID, GetGinRequestID）
  </action>
  <verify>
    1. 运行 `go build ./internal/logging/...` 确保编译通过
    2. 检查修改后的代码符合上述要求
  </verify>
  <done>
    - GenerateRequestID() 生成 16-char hex
    - fallback 返回 16 zeros
    - 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加/更新单元测试</name>
  <files>internal/logging/requestid_test.go</files>
  <action>
创建或更新 internal/logging/requestid_test.go：

1. 测试生成长度：验证 GenerateRequestID() 返回长度为 16 的字符串
2. 测试字符集：验证返回字符串只包含 [0-9a-f] 字符
3. 测试唯一性：生成 1000 个 ID，验证无重复（概率性测试）
4. 测试并发安全：启动 100 个 goroutine 各生成 100 个 ID，验证无重复且长度正确
5. 如果测试文件已存在，更新现有测试以匹配新的 16-char 长度

测试模板：
```go
package logging

import (
	"regexp"
	"sync"
	"testing"
)

func TestGenerateRequestID_Length(t *testing.T) {
	id := GenerateRequestID()
	if len(id) != 16 {
		t.Errorf("expected length 16, got %d", len(id))
	}
}

func TestGenerateRequestID_HexFormat(t *testing.T) {
	id := GenerateRequestID()
	matched, _ := regexp.MatchString("^[0-9a-f]+$", id)
	if !matched {
		t.Errorf("expected hex format, got %s", id)
	}
}

func TestGenerateRequestID_Uniqueness(t *testing.T) {
	seen := make(map[string]bool)
	for i := 0; i < 1000; i++ {
		id := GenerateRequestID()
		if seen[id] {
			t.Errorf("duplicate ID generated: %s", id)
		}
		seen[id] = true
	}
}

func TestGenerateRequestID_Concurrent(t *testing.T) {
	var wg sync.WaitGroup
	ids := make(chan string, 10000)
	
	for i := 0; i < 100; i++ {
		wg.Add(1)
		go func() {
			defer wg.Done()
			for j := 0; j < 100; j++ {
				ids <- GenerateRequestID()
			}
		}()
	}
	
	wg.Wait()
	close(ids)
	
	seen := make(map[string]bool)
	for id := range ids {
		if len(id) != 16 {
			t.Errorf("wrong length: %s (%d chars)", id, len(id))
		}
		if seen[id] {
			t.Errorf("duplicate in concurrent test: %s", id)
		}
		seen[id] = true
	}
}
```
  </action>
  <verify>
    1. 运行 `go test ./internal/logging/... -v` 确保所有测试通过
    2. 验证测试覆盖新行为（16-char 长度）
  </verify>
  <done>
    - 测试文件存在且通过
    - 测试覆盖长度、格式、唯一性、并发安全
  </done>
</task>

</tasks>

<verification>
1. 代码审查：
   - GenerateRequestID() 使用 8 bytes (64-bit)
   - 返回 16-character hex 字符串
   - fallback 值为 16 zeros

2. 测试验证：
   - `go test ./internal/logging/... -v` 全部通过
   - 测试输出显示 1000+ 个唯一 ID 生成成功

3. 集成检查（可选）：
   - 运行 `go build ./...` 确保整个项目编译通过
</verification>

<success_criteria>
- GenerateRequestID() 生成 16-char hex (64-bit)
- 单元测试通过，覆盖长度、格式、唯一性、并发
- 项目编译通过
- 向后兼容：不影响现有 8-char ID 的查询（DB 层保持不变）
</success_criteria>

<output>
After completion, create `.planning/phases/10-request-id-robustness/10-01-SUMMARY.md`
</output>

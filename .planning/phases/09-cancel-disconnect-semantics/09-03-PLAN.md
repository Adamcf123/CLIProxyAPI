---
phase: 09-cancel-disconnect-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - internal/metricsruntime/request_state_test.go
  - internal/metricsruntime/usage_plugin_test.go
  - internal/api/middleware/response_writer_test.go
autonomous: true

must_haves:
  truths:
    - "测试覆盖 non-streaming 的 cancel/disconnect，并锁定 outcome=canceled"
    - "测试锁定：timeout 归类为 failure，而不是 canceled"
    - "测试锁定：canceled 的持久化信号为 status_code=499，且 per-request canceled error 为空（error_info 为空）"
    - "作为回归 gate：全量 go test ./... 通过（不允许留下隐式失败）"
  artifacts:
    - path: "internal/api/middleware/response_writer_test.go"
      provides: "non-streaming write error -> canceled 归类测试"
    - path: "internal/metricsruntime/request_state_test.go"
      provides: "写入层 outcome 优先级（failure > canceled > success）测试"
    - path: "internal/metricsruntime/usage_plugin_test.go"
      provides: "持久化映射：canceled -> 499 且 error_info=nil 的测试"
  key_links:
    - from: "internal/metricsruntime/usage_plugin_test.go"
      to: "internal/metricsruntime/usage_plugin.go"
      via: "断言 canceled 行不写 error_info 且 status_code=499"
      pattern: "ErrorInfo|StatusCode"
---

<objective>
用测试把 Phase 09 的 cancel/disconnect 写入侧语义锁死，覆盖 non-streaming 的断连检测路径，并作为全量回归 gate。

Purpose: 防回归；让后续优化（Phase 10/11）不破坏 canceled 语义
Output: 写入侧单元测试 + non-streaming 断连归类测试 + 全量回归 gate（Query API 契约已在 09-02 锁定）
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cancel-disconnect-semantics/09-CONTEXT.md
@.planning/phases/09-cancel-disconnect-semantics/09-RESEARCH.md

@internal/metricsruntime/request_state.go
@internal/metricsruntime/usage_plugin.go
@sdk/api/handlers/stream_forwarder.go
@internal/api/middleware/response_writer.go
@internal/api/handlers/management/metrics.go

@internal/metricsruntime/request_state_test.go
@internal/metricsruntime/usage_plugin_test.go
@internal/api/middleware/response_writer_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 单元测试锁定 canceled 的写入优先级与持久化映射</name>
  <files>
internal/metricsruntime/request_state_test.go
internal/metricsruntime/usage_plugin_test.go
  </files>
  <action>
补齐单元测试，锁定写入层的关键不变量：

- `internal/metricsruntime/request_state_test.go`：新增/扩展测试覆盖：
  - 标记 canceled 后，再调用 `SetStatusCode(200)` 不会覆盖回 success（防 handler 尾部覆盖）。
  - failure 优先：若已标记 canceled，随后出现明确 failure（例如 `SetLastError(errors.New("boom"))` 或 `SetStatusCode(500)`），最终应表现为 failure（避免 upstream error 被 canceled 吞掉）。
  - timeout 路径（若 RequestState 提供专用 API）：确保 timeout 不会调用 canceled 分支。

- `internal/metricsruntime/usage_plugin_test.go`：新增测试覆盖：
  - 当 RequestState 为 canceled（status_code=499）时：
    - `MetricRecord.StatusCode` 为 499
    - `MetricRecord.ErrorInfo` 为 nil（per-request canceled error 为空的持久化基础）
    - TPSCollector 不会被调用（若实现中采用“canceled 不聚合”策略，则用测试缝/spy 锁定）

注意：不新增对外参数；测试只锁定语义，不引入兼容模式。
  </action>
  <verify>
go test ./internal/metricsruntime
rg -n "499|canceled" internal/metricsruntime/request_state_test.go internal/metricsruntime/usage_plugin_test.go
  </verify>
  <done>
- 写入层不变量被测试锁定：canceled 不可被 2xx 覆盖；failure 优先；canceled 的 ErrorInfo 必须为空
  </done>
</task>

<task type="auto">
  <name>Task 2: non-streaming 取消断连（write error）路径的归类测试</name>
  <files>
 internal/api/middleware/response_writer_test.go
  </files>
  <action>
 覆盖 non-streaming 的断连检测路径（write error -> canceled）的可重复测试：

 - 新增 `internal/api/middleware/response_writer_test.go`：
   - 实现一个 fake `gin.ResponseWriter`，其 `Write/WriteString` 返回固定 error（模拟 client gone）。
   - 通过 `NewResponseWriterWrapper` + `Finalize(c)` 驱动，断言 RequestState 被标记为 canceled（status_code=499，且不通过 error_info/LastError 表达）。
   - 增加 timeout 分支：将 request context 设置为 DeadlineExceeded（或可控超时），断言 Finalize 标记 failure（不得是 499）。

 要求：测试避免依赖 wall-clock；尽量用 context cancel/deadline 触发，不用 sleep。
  </action>
  <verify>
 go test ./internal/api/middleware -run Test
  </verify>
  <done>
 - non-streaming 的 canceled 与 timeout/failure 归类被测试锁定
  </done>
</task>

<task type="auto">
  <name>Task 3: 全量回归 gate（不引入隐式依赖）</name>
  <files>
internal/metricsruntime/request_state_test.go
internal/metricsruntime/usage_plugin_test.go
internal/api/middleware/response_writer_test.go
  </files>
  <action>
 运行全量测试作为回归 gate，确保 Phase 09 的三个 Plans 之间不存在“测试更新滞后”的隐式依赖：

 - 执行 `go test ./...`，要求全量通过。
 - 若出现失败：
   - 只在本 Plan 的 `files_modified` 内修复测试（优先修复断言或 test seam），避免扩散到无关包。
   - 不新增 endpoint / query 参数；不改变锁定语义：
     - cancel/disconnect=canceled；timeout=failure；upstream error=failure
     - percentiles 排除 canceled，且显式体现 canceled_count；buckets 每 bucket 有 canceled_count
     - per-request canceled error 为空
  </action>
  <verify>
 go test ./...
  </verify>
  <done>
 - 全量 `go test ./...` 通过，作为 Phase 09 的回归 gate
  </done>
</task>

</tasks>

<verification>
- `go test ./...` 全量通过（本 Plan 即为回归 gate）
- 写入侧与 non-streaming 的关键语义均有明确断言：failure > canceled > success；canceled -> 499 且 error_info=nil；timeout 不会被误标 canceled
</verification>

<success_criteria>
- cancel/disconnect 的三分法语义在写入层与查询层完全一致，并由测试锁死
- 未来任何把 canceled 误归类为 success/failure 的改动都会被测试立即发现
</success_criteria>

<output>
After completion, create `.planning/phases/09-cancel-disconnect-semantics/09-03-SUMMARY.md`
</output>
